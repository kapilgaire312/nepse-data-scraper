# Nepse API Scraper

This project is a small Python-based scraper for Nepse data. The goal is to collect market information by using available APIs and build a clean dataset that is served through a FastAPI backend in a separate project.
